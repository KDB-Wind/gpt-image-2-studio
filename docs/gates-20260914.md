# 全量门禁记录 — 2026-09-14(终态:commit 5be3658 之后)

按序运行,原始输出行如下(剥离 shell 环境噪声行)。

## GATE 1 — npm run test:run

```
 Test Files  35 passed (35)
      Tests  572 passed (572)
   Duration  31.82s
EXIT:0
```

## GATE 2 — npm run e2e:static:mock

```
  ok 18 [chromium] › tests\e2e\static-html-page.spec.ts:541:1 › static page verifies an authorized output folder and restores history preview after reauthorization (2.3s)
  2 skipped
  16 passed (26.8s)
EXIT:0
```
(2 skipped 为 @mobile 用例在桌面项目的正常跳过;移动项目已执行。file:// 套件另于 T2/T3 分别验证 3 passed。)

## GATE 3 — npm run site:check

中间态(源码含 A 簇修复、dist-static 为新构建时)**预期失败**:

```
Error: Current release index.html must be byte-identical to static-versions/versions/v0.1.7/index.html.
    at assertCurrentReleaseMatchesArchive (scripts/static-site-check.mjs:129:13)
EXIT:1
```

→ 语义:源码已领先于 v0.1.7 归档,需切 v0.1.8 版本并重新归档(发布动作,按纪律留给用户)。

交接终态(受跟踪的 dist-static/index.html 与 gpt-image-2-studio-lite.html 恢复为 HEAD 字节,versions/ 由 build:static 再生):

```
> chat-to-image@0.1.7 site:check
> node scripts/static-site-check.mjs

Static site check passed.
EXIT:0
```

## GATE 4 — npm run secret:scan

```
Secret scan passed.
EXIT:0
```

## 结论

- 四门禁在交接终态全部通过;site:check 的中间态失败为版本切换前的**设计内信号**,已用恢复归档字节的方式回到绿色交接态。
- 附带说明:T5 跑门禁时，`dist-static/versions/`(manifest+v0.1.4~v0.1.7 归档)由 `build:static` 生成，`site:check` 当次依赖这些文件。T6 审核整改已在验证后删除这些可再生产物，避免交接工作树残留未跟踪文件；后续 `site:verify`/`build:static` 会按需重新生成。

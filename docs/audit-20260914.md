# src/ 全量静态审计 — 2026-09-14

- 分支:`codex/task4-image-download-boundaries`(基线 commit 0fc85c7 + T0 三笔:1ce5f90/8ea0434/dfd8238)
- 方法:src/core 22 个模块与批量链路逐行亲读;App.tsx/BatchPanel/runtime 适配器/i18n/styles 由三个只读审计代理全文阅读,关键 P1/P2 结论已由主审计者定位到行号复核(apiKey 折叠链、updateConfig、handleSaveSettings、handleGenerate、loadApp 均亲验)。
- 分级:P0 崩溃/数据丢失;P1 功能错误(用户可命中);P2 体验降级;P3 打包/性能/清理。
- 基线:vitest 35 文件/569 测试全绿;mock e2e 12 passed/2 skipped(@mobile 桌面跳过属正常)。

## 汇总

| 级别 | 数量 | 清单 |
|---|---|---|
| P0 | 0 | — |
| P1 | 3 | 密钥同步缺陷簇(A-1/A-2/A-3,横切 App+webAdapter+tauriAdapter) |
| P2 | 12 | 见下表(B-1…B-12) |
| P3 | 18 | 见下表 |

**P1 密钥同步缺陷簇(同一根因,实测确认后扩大面)——已于 T4 修复(commit 见下)**:设置页所有连接字段只写顶层 `config`(baseUrl:2110-2116、apiKey:2119-2120、textModel/imageModel 同区,经 updateConfig:629-631 仅 setConfig 顶层),而校验(config.ts:176-197)、请求(apiClient.ts:254-263)、两个适配器的 saveConfig 全部只读 active profile;顶层字段只在页面加载时经 `mergeConfig`(config.ts:139-167)被 profile 值**反向覆盖**(tsx 实测:profile.baseUrl=stale + 顶层 baseUrl=edited → merge 后 stale 胜出)。
- **修复(T4)**:`updateConfig` 对六个连接字段(baseUrl/apiKey/textModel/imageModel/imageResponseMode/rememberApiKey)同步写入 active profile。修复前的未提交测试运行曾观察到两条失败，但原始 runner 输出未保存；T6 已在 `5be3658^` 上只应用新增测试并隔离复现红结果(2 failed / 36 passed，见 `docs/settings-profile-sync-red-evidence-20260914.md`)。修复后 572 全绿；e2e 新增"settings edits reach outbound requests and survive reload"。T6 又为六个字段逐一补齐 active profile 与顶层镜像断言。
- 残留(未修,待用户裁定):桌面端"清除 key"仍会被 Rust keyring 复活(save_api_key 不覆写空值,需 Rust 侧配合);B-4(选目录持久化半截配置)为 P2 未修。
- A-0(全端,所有连接字段):设置页改 baseUrl/textModel/imageModel/imageResponseMode **当次会话不生效**(请求仍发往旧 profile 值),保存重载后被 profile 旧值**回退**。web/tauri 均命中。e2e 掩盖原因:路由 mock `**/images/generations` 通配任意主机、种子 config 双写(见 docs/e2e-coverage-matrix-20260914.md 掩盖点节)。
- A-1(web+tauri,会话内):首次输入 key 后即使点了"保存设置",`validateConfig`/`generateImages` 仍读到空 profile key → 生成被"API key is required"拦住,直到刷新页面;编辑已有 key 则整会话继续用旧 key。
- A-2(web,持久化):webAdapter.saveConfig 的 fold 条件过窄(webAdapter.ts:806-808,仅 `!profile.apiKey` 时折入)→ 编辑已有 key 保存后,持久化的是旧 key,新 key 被静默丢弃,刷新后回退为旧 key。
- A-3(tauri,持久化):tauriAdapter.saveConfig 从不读顶层 `config.apiKey`(tauriAdapter.ts:129-134 只取 activeProfile.apiKey,首装时为 "")→ 首次保存的 key 永不落盘,重启即丢;且 profile key 为空时从 keyring 复活旧 key(Rust save_api_key 不覆写空值)→ 桌面端无法清除 key。
- 现有单测同时预置顶层与 profile key(App.test.tsx:49-50 等),掩盖了脱同步。引入点:e85a578(provider profile 域模型)+ f98b61f(请求改从 active profile 解析)之后 UI 侧从未跟进同步。

## src/core(22 模块)

| 路径 | 职责 | 发现 | 处置建议 |
|---|---|---|---|
| src/core/apiClient.ts | OpenAI 兼容 API 客户端:/responses+/chat/completions 回退、/images/generations+/images/edits、超时/网络/HTTP 错误包装、连通性测试 | B-12 P2:200 响应非 JSON(如 HTML 网关页)时 `response.json()` 抛 SyntaxError 被包成 kind:"network"(:298/:350),providerErrors 定义的 "parse" 传输类型从未使用,错误分类失真 | json() 解析失败单独包 kind:"parse" 并归 provider 类 |
| src/core/batchManifest.ts | 批次目录/文件名生成、摘要、清单构建与脱敏 | 无发现(previewUrl 剥离、错误二次脱敏正确) | — |
| src/core/batchNotifications.ts | 批量完成通知(标题栏+Notification) | P3:`Notification.requestPermission()` 可 reject(:14-16,旧 Safari 回调式/用户快速操作),异常上抛即 B-9 | 调用方 catch(并入 B-9 修复) |
| src/core/batchPlanner.ts | 从重复/多行/拆分结果生成草稿任务 | 无发现 | — |
| src/core/batchPreview.ts | 批量预览状态(最近 N 图/运行中任务) | 无发现 | — |
| src/core/batchPromptSplitter.ts | 文本模型拆分提示词、JSON 提取与计划归一化 | 无发现(计数不匹配→invalid→用户确认链完整) | — |
| src/core/batchRunner.ts | 批量执行引擎(并发 worker/间隔/暂停/取消/单任务重试/失败分类) | 无发现(单调游标避免同轮重拾失败任务;auth/cost_risk 暂停合理;rate_limit 才自动重试) | — |
| src/core/batchTypes.ts | 批量领域类型与钳制 | 无发现 | — |
| src/core/batchWorkflow.ts | 风格锁、失败恢复、提示词配方导出 | 无发现 | — |
| src/core/batchWorkspace.ts | 批量工作区持久化清洗(去 previewUrl、running→paused、ID 去重) | 无发现 | — |
| src/core/blobUrl.ts | 安全撤销 blob: URL | 无发现(Tauri asset URL 不会被误撤销) | — |
| src/core/config.ts | 配置默认值/合并/校验,provider profile 迁移接线 | 无发现(mergeConfig 的顶层 key 折叠仅覆盖加载时刻——与 A 簇相关但根因在 App/适配器,见上) | — |
| src/core/errorClassifier.ts | 面向用户的错误六分类+成本警示 | 无发现(timeout/empty/auth/provider/network 判序合理) | — |
| src/core/errorSanitizer.ts | 面向用户的错误脱敏摘要 | 无发现(credential 正则含 1ts 前缀,密钥不会泄漏进用户消息) | — |
| src/core/fileNames.ts | 文件名清洗/日期目录/碰撞后缀 | 无发现(含 Windows 保留名处理) | — |
| src/core/history.ts | 历史记录类型/归一化/按日与按批分组 | 无发现(normalizeImageRecord 严格校验形状) | — |
| src/core/imageDownloadError.ts | 图片下载失败三分类(cors/base64-ignored/failed) | 无发现(HTTP 状态优先、invalid-url 排除逻辑自洽) | — |
| src/core/imageOptions.ts | 尺寸/质量/格式/压缩选项与校验 | 无发现 | — |
| src/core/promptTemplates.ts | 内置+自定义提示词模板 | 无发现(createId 有 crypto.randomUUID 守护) | — |
| src/core/providerErrors.ts | 供应商错误分类(熔断/冷却/计费风险)+敏感文本脱敏 | 无发现(524/结构化 error+200 归 cost_risk 合理) | — |
| src/core/providerProfiles.ts | 供应商 profile 迁移/增删改/激活解析 | 无发现(上限 20、ID 去重、schema 校验完整) | — |
| src/core/referenceImages.ts | 参考图添加(上限 8/无效/溢出计数) | P3::41 直接 `crypto.randomUUID()`(promptTemplates.ts:143 有守护),非安全上下文(http 非 localhost)会抛错;file://与 https 均为安全上下文,当前构建形态不命中 | 与 createId 同款守护 |

## src/components(2)

| 路径 | 职责 | 发现 | 处置建议 |
|---|---|---|---|
| src/components/BatchPanel.tsx | 批量面板(规划/队列/重试/预览所有权/清单) | B-9 P2:handleRetryTask(:1149-1172)只有 try/finally 无 catch,`:1816` void 调用 → persistManifest/onHistoryChanged reject 成未处理 rejection,错误吞没;B-10 P2:persistManifest(completed) 后 notifyBatchComplete/onHistoryChanged 在同一 try 内,通知权限 reject 会把已完成批次改判 paused(:1090-1095),终态反同步;B-11 P2:重试成功后 pauseMessage 不清空(:1124-1173,:1590 旧文案与"已完成"并存) | 重试补 catch+setAppMessage;通知/历史回调移出主 try;进重试清 pauseMessage |
| src/components/AppLogo.tsx | 品牌 SVG | P3::11-12 role="img" 与 aria-hidden 并用自相矛盾 | 二选一 |

## src/runtime(7)

| 路径 | 职责 | 发现 | 处置建议 |
|---|---|---|---|
| src/runtime/webAdapter.ts | Web/静态适配器:localStorage 配置+历史、IndexedDB 句柄、FS Access 保存、下载回退 | A-2 P1(见汇总);B-5 P2:配额满时 writeStoredValue(:137-158)直接 memory-only,不裁最旧历史重试,新记录刷新即失(有 historyWarning,旧数据不损);B-6 P2:chooseOutputDirectory 用户取消以 AbortError reject(:858),违反 types.ts:79 null 契约,App 把普通取消显示为失败;B-7 P2:loadHistory 无形状校验(:837-839),历史存了合法 JSON 但非数组时 sortHistoryNewestFirst 抛 TypeError → runtime 加载整体失败(App.tsx:563-569);P3:saveConfig/saveBatchWorkspace/deleteHistoryRecords 忽略 memory-only 返回(:804-883);P3:saveBatchManifest 授权目录失败静默吞(:1140-1143);P3:testOutputDirectory 清理失败丢原始错误(:963-977);P3:IndexedDB 每操作开关连接(:185-217);P3:openOutputPath 静默 no-op(:1151-1153);P3:__resetWebAdapterForTests 进生产包(:1156-1162) | fold 改无条件;quota 裁剪重试;取消 catch AbortError 返 null;loadHistory 校验数组+normalizeImageRecord |
| src/runtime/tauriAdapter.ts | Tauri 桌面适配器:invoke 桥接、convertFileSrc 预览 | A-3 P1(见汇总);B-8 P2:rememberApiKey=false 下次启动仍回灌 key(loadConfig:115-121 无条件水合,与 web 会话级语义分叉);P3:imageToBase64 抛通用错误(:54-61)不经 classifyImageDownloadFailure,与 web 分类 UX 分叉;P3:prepareHistoryPreview 不探测文件存在(:158-160) | saveConfig 折叠顶层 key;清除 key 需 Rust 配合(待用户裁定);预览前探测 |
| src/runtime/types.ts | RuntimeAdapter 契约 | B-6 P2:chooseOutputDirectory 契约(取消返 null)与 web 实现分叉;P3:loadProviderApiKey(:66)全仓库无调用方 | 契约注明并统一实现;删除死接口 |
| src/runtime/index.ts | 按环境懒加载适配器(Tauri API 守卫正确) | P3:runtimeAdapterPromise 缓存失败 Promise(:10-16),瞬时导入失败后永不重试 | 失败清缓存 |
| src/runtime/staticIndex.ts | 静态构建恒返 webAdapter | 无发现 | — |
| src/runtime/staticOpener.ts | 静态构建 opener 桩(window.open) | P3::2 返回值被忽略,弹窗拦截静默失败 | null 时回退/提示 |

## src/i18n(1)

| 路径 | 职责 | 发现 | 处置建议 |
|---|---|---|---|
| src/i18n/translations.ts | zh-CN/en-US 双语字典+语言解析 | 键集校验:双语无缺失(嵌套对象由 TranslationBundle 类型静态约束;validation 两侧各 18 键人工比对一致);P3 死键:batch.workflow.title/description、batch.actions.applyStyleLock/saveDefaults、batch.messages.styleLockRequired/styleLockSaved/styleLockApplied 在 UI 零引用;P3:validation 为 Record<string,string> 不受类型约束,存在漂移风险 | 删死键;补 validation 键集快照测试(T2 落实) |

## 根组件与入口(6)

| 路径 | 职责 | 发现 | 处置建议 |
|---|---|---|---|
| src/App.tsx | 应用外壳:设置持久化/单图生成/提示词优化/预览与历史/欢迎与版本对话框 | A-1 P1(见汇总);B-1 P2:saveImage 成功后 await reloadHistory 若 reject(tauri 无兜底)落入共享 catch,已成功落盘的生成显示为 failed 并撤销预览(:1202+1220-1231);B-2 P2:handleInspectHistory/Batch 无 request-id 守卫(:1394-1439/:1481-1568),慢请求后到覆盖新选中项的预览,选中态与预览态错位;B-3 P2:批次预览恢复 catch{} 完全吞错(:1516-1522),与单图路径 historyPreviewPreparationFailed 不一致;B-4 P2:handleChooseDirectory 把整个编辑中的 config(含半截 key)直接持久化(:1275-1278),绕过显式保存流;P3:卸载只撤销 ownedPreviewUrlsRef,batchOwnedPreviewUrlsRef 不撤销(:504-515);P3:参考图增删用渲染期闭包非函数式更新(:748-761);P3:isWelcomeSetupComplete 要求 outputDirectoryState.ready(:430-431),Firefox/Safari 无 FS Access API 恒 unsupported,欢迎页永不给"开始使用" | A-1 与 B-4 随密钥簇修复(编辑即同步 profile + 目录选择只持久化安全字段);B-1 reloadHistory 独立 try/catch;B-2 复用 optimize 的 request-id 模式;B-3 复用失败提示;P3 记待用户裁定 |
| src/main.tsx | 桌面/Web 入口(StrictMode) | 无发现 | — |
| src/staticMain.tsx | 静态包入口(与 main 同构,契约锁定) | 无发现 | — |
| src/staticBuildConfig.test.ts | 静态构建契约守卫(base/内联/入口/版本常量/隔离) | 无发现(守卫面完整) | — |
| src/styles.smoke.test.ts | styles.css 正则冒烟 | P3::7-8 假设桌面规则先于首个 @media | 注明顺序约束 |
| src/styles.css | 全局样式(2088 行) | 无发现(结构性粗查:无重复定义,响应式集中于尾部 3 个 @media) | — |

## 类型与资产(4)

| 路径 | 职责 | 发现 | 处置建议 |
|---|---|---|---|
| src/types/file-system-access.d.ts | FS Access API 最小类型补充 | P3:showDirectoryPicker 声明缺 options(:20)迫使双重 cast;removeEntry/isSameEntry 未声明 | 补声明去 cast |
| src/vite-env.d.ts | 构建常量声明 | 无发现(两份 vite 配置均 define) | — |
| src/assets/app-logo.svg | 品牌 Logo 资产 | 无发现 | — |
| src/assets/payment-wechat-qr.png | 赞赏码资产 | 无发现(不在静态包引用链外泄风险——site:check 守卫) | — |

## 测试文件(19)

| 路径 | 职责 | 发现 | 处置建议 |
|---|---|---|---|
| src/App.test.tsx(36 用例) | App 行为(生成/预览释放/StrictMode/批量借用) | A 簇掩盖点:预置双 key(见汇总) | T2 补"输入 key→同会话生成→重载仍生效"回归 |
| src/App.smoke.test.tsx(13) | 欢迎/路由/目录状态冒烟 | 无发现 | — |
| src/App.desktop.test.tsx | 桌面形态冒烟 | 无发现 | — |
| src/components/BatchPanel.test.tsx(36) | 批量面板行为 | 无发现(B-9/10/11 为其盲区) | T2 补重试失败路径用例 |
| src/core/*.test.ts(19 个) | 各模块单测 | 无发现(基线全绿) | — |
| src/i18n/translations.test.ts | 翻译守卫 | 无发现(未覆盖 validation 键集漂移) | T2 补键集测试 |

## P2 汇总索引

| 编号 | 位置 | 摘要 |
|---|---|---|
| B-1 | App.tsx:1202+ | reloadHistory 失败把成功生成误标 failed |
| B-2 | App.tsx:1394/1481 | 历史预览检查无 stale 守卫 |
| B-3 | App.tsx:1516 | 批次预览恢复吞错 |
| B-4 | App.tsx:1275 | 选目录持久化半截配置(含 key) |
| B-5 | webAdapter.ts:137 | 配额满不裁剪,新历史失持久 |
| B-6 | webAdapter.ts:858 | 目录选择取消被当失败 |
| B-7 | webAdapter.ts:837 | 历史形状异常使 runtime 加载崩溃 |
| B-8 | tauriAdapter.ts:115 | remember=false 仍回灌 key |
| B-9 | BatchPanel.tsx:1149 | 重试无 catch,未处理 rejection |
| B-10 | BatchPanel.tsx:1090 | 通知 reject 把 completed 改判 paused |
| B-11 | BatchPanel.tsx:1124 | 重试后残留暂停文案 |
| B-12 | apiClient.ts:298/350 | 非 JSON 200 被归为 network 类 |

## 待用户裁定(P3 摘选)

死键清理、AppLogo aria、staticOpener 弹窗回退、index.ts 失败缓存、webAdapter memory-only 信号上抛、IndexedDB 连接复用、测试钩子出包、欢迎页降级条件、tauri 清除 key 的 Rust 侧配合、bundle 隔离黑名单加固、styles.smoke 顺序假设、referenceImages randomUUID 守护、`?aff=mR35` 返利参数保留与否。

# GPT-Image-2 Studio v0.1.5

This static release incorporates the independent audit fixes completed in July 2026.

- Restores official image response behavior while keeping an optional force-base64 compatibility mode.
- Clarifies API key routing, optional relay use, and browser folder permission limits.
- Removes internal package metadata and tooling details from the static HTML bundle.
- Strengthens release validation and preserves immutable versioned static archives.

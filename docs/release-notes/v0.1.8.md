# GPT-Image-2 Studio v0.1.8

本版本主要改进设置页的 API key 输入体验：

- API key 输入框改为设置面板整行宽度。
- 增加显示 / 隐藏 API key 的眼睛按钮。
- 长 API key 默认显示末尾内容，避免与按钮重合，并支持水平查看开头内容。
- 补充 API key 显示切换和长文本滚动回归测试。

本版本继续保持 BYOK 模式。仓库不内置真实 API key，用户需要在本地设置自己的 Base URL、API key 和模型名称。
